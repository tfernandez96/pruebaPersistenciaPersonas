package muertos.andantes.modelo.persistencia;

public enum  Estado {
	UN_ESTADO, OTRO_ESTADO
	//El estado era una interfaz de la cual no se tenian muchos datos, aparentemente no tiene parametros,
	//Por lo que vamos a ir por hacerlo un enum.
	//Si tuviera atributos, optaría por una Single Table porque el uso va a ser polimorfico.
	
}
